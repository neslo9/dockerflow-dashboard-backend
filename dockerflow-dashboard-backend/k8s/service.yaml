apiVersion: v1
kind: Service
metadata:
  name: dockerflow-backend
  namespace: default
spec:
  selector:
    app: dockerflow-backend
  type: NodePort      
  ports:
    - protocol: TCP
      port: 80          
      targetPort: 8000  
      nodePort: 30008   
